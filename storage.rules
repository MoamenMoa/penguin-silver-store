rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Firebase Storage is intentionally disabled in this build.
    // Product images are delivered from ImageKit and only their URLs are stored in Firestore.
    match /{allPaths=**} { allow read, write: if false; }
  }
}
