export function colorsOf(p){return Array.isArray(p.colors)?p.colors.filter(c=>c&&typeof c.name==='string'&&c.name.trim()&&c.name.length<=40&&/^#[0-9a-f]{6}$/i.test(c.hex)).slice(0,8):[];}
export function swatches(p,onChoose){const wrap=document.createElement('div');wrap.className='color-swatches';wrap.setAttribute('aria-label','ألوان المنتج');for(const c of colorsOf(p)){const b=document.createElement(onChoose?'button':'span');b.className='color-swatch';b.style.backgroundColor=c.hex;b.title=c.name;b.setAttribute('aria-label',c.name);if(onChoose){b.type='button';b.setAttribute('aria-pressed','false');b.onclick=()=>{wrap.querySelectorAll('button').forEach(x=>x.setAttribute('aria-pressed',String(x===b)));onChoose(c.name);};}wrap.append(b);}return wrap;}
